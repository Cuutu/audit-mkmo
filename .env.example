# =============================================================================
# Programa de Auditoría - Variables de Entorno
# =============================================================================
# Copiar a .env y completar con valores reales. NUNCA commitear .env
# =============================================================================

# Base de datos MongoDB (requerido)
DATABASE_URL="mongodb://localhost:27017/auditoria_obras"

# NextAuth (requerido)
# Generar secret: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Entorno
NODE_ENV="development"

# Archivos (Vercel Blob en producción - el token se inyecta automáticamente)
# BLOB_READ_WRITE_TOKEN  <- Vercel lo añade si usas Vercel Blob
MAX_FILE_SIZE="52428800"
ALLOWED_FILE_TYPES="pdf,doc,docx,xls,xlsx,jpg,jpeg,png"

# Google Maps (opcional - para autocomplete de ubicación)
# Restringir por dominio en Google Cloud Console
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""

# Seed (requeridos para npm run db:seed)
SEED_ADMIN_EMAIL="admin@auditoria.com"
SEED_ADMIN_PASSWORD="cambiar-en-primera-ejecucion"
